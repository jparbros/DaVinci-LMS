= widget_div do
  .title 
    span A teacher in these courses
    = link_to 'Add'
  
  table.table.table-bordeless
    tbody
      - user.teacher_in.each do |course|
        tr
          td = link_to course.full_name, management_course_path(course.id)